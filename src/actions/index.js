//this file holds all of the action creators

import types from './types';

export function location() {

    //write function that will return a new state for location

    return {
        type: types.LOCATION
    }
}